# Reusable UI Components

Folder ini digunakan untuk menampung komponen UI yang dapat digunakan kembali (reusable) di berbagai screen aplikasi LaviraMeal.

Contoh komponen:
- Custom Button (`Button.js`)
- Custom TextInput (`Input.js`)
- Card (`Card.js`)
- Loading Indicator (`Loading.js`)
